# Docker — INFI TV

Compose completo (backend, postgres, redis, admin, nginx) en **FASE 19**.

Estructura prevista:

```text
docker/
├── backend.Dockerfile   # FASE 19
├── admin.Dockerfile     # FASE 19
└── nginx/
    └── nginx.conf       # FASE 19
```
